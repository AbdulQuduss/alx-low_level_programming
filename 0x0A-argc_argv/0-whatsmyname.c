#include <stdio.h>
#include "main.h"
/**
 * main- prints the name of the program
 * @argv: argument array
 * Return: always 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (argc);
}
